import localFont from "next/font/local";

export const fontHailey = localFont({
  src: [{ path: "../fonts/hailey.ttf" }],
  display: "swap",
});

export const fontItaliana = localFont({
  src: [{ path: "../fonts/italiana.ttf" }],
  display: "swap",
});
